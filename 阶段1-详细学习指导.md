# 阶段1：基础概念与架构 - 详细学习指导

## 学习目标

1. 理解BeanFactory作为Spring容器的根接口的设计思想
2. 掌握ListableBeanFactory的枚举能力
3. 理解BeanDefinition作为Bean元数据的核心作用
4. 能够手动使用DefaultListableBeanFactory创建和管理Bean

预计学习时间：3-4天

---

## Day 1: BeanFactory 接口深度解析

### 核心文件
`spring-beans/src/main/java/org/springframework/beans/factory/BeanFactory.java`

### 关键要点

#### 1. 接口定位（第24-26行注释）
```java
/**
 * The root interface for accessing a Spring bean container.
 * 访问Spring bean容器的根接口
 */
```
**理解**：这是整个Spring IoC容器的最基础接口，所有其他容器接口都继承或扩展它。

#### 2. FactoryBean前缀常量（第129行）
```java
String FACTORY_BEAN_PREFIX = "&";
```
**重点理解**：
- 当bean名称前加`&`时，获取的是FactoryBean本身，而不是它创建的对象
- 例如：`myFactoryBean`获取的是工厂创建的对象，`&myFactoryBean`获取的是工厂本身

#### 3. 核心方法分类

##### 获取Bean的方法（第153-264行）

| 方法签名 | 作用 | 使用场景 |
|---------|------|---------|
| `getBean(String name)` | 按名称获取 | 知道bean名称时 |
| `getBean(String name, Class<T> requiredType)` | 按名称+类型获取 | 需要类型安全时 |
| `getBean(String name, Object... args)` | 带参数获取 | 需要传入构造参数时 |
| `getBean(Class<T> requiredType)` | 按类型获取 | 按类型自动装配 |
| `getBean(Class<T> requiredType, Object... args)` | 按类型+参数获取 | 按类型获取并传参 |
| `getBeanProvider(Class<T> requiredType)` | 获取延迟加载提供者 | 需要延迟获取或可选依赖时 |

**学习重点**：
- 第174行的`<T> T getBean(String name, Class<T> requiredType)`是类型安全版本
- 第241行的`getBeanProvider`支持延迟加载（Spring 5.1新增）

##### 查询Bean的方法（第281-402行）

| 方法 | 作用 |
|------|------|
| `containsBean(String name)` | 检查是否包含bean |
| `isSingleton(String name)` | 是否是单例 |
| `isPrototype(String name)` | 是否是原型 |
| `isTypeMatch(String name, Class<?> typeToMatch)` | 类型是否匹配 |
| `getType(String name)` | 获取bean类型 |
| `getAliases(String name)` | 获取别名列表 |

#### 4. 生命周期文档（第67-96行注释）⭐⭐⭐

这部分是**最重要的文档**，详细描述了Bean的完整生命周期：

**初始化阶段（按顺序）**：

1. `BeanNameAware.setBeanName()` - 注入bean名称
2. `BeanClassLoaderAware.setBeanClassLoader()` - 注入类加载器
3. `BeanFactoryAware.setBeanFactory()` - 注入BeanFactory
4. `EnvironmentAware.setEnvironment()` - 注入环境（Context中）
5. `EmbeddedValueResolverAware.setEmbeddedValueResolver()` - 注入值解析器
6. `ResourceLoaderAware.setResourceLoader()` - 注入资源加载器
7. `ApplicationEventPublisherAware.setApplicationEventPublisher()` - 注入事件发布器
8. `MessageSourceAware.setMessageSource()` - 注入消息源
9. `ApplicationContextAware.setApplicationContext()` - 注入应用上下文
10. `ServletContextAware.setServletContext()` - 注入ServletContext
11. `BeanPostProcessor.postProcessBeforeInitialization()` - 初始化前处理
12. `InitializingBean.afterPropertiesSet()` - 初始化回调
13. `init-method` - 自定义初始化方法
14. `BeanPostProcessor.postProcessAfterInitialization()` - 初始化后处理

**销毁阶段**：
1. `DestructionAwareBeanPostProcessor.postProcessBeforeDestruction()`
2. `DisposableBean.destroy()`
3. `destroy-method`

**学习建议**：
- 把这14个初始化步骤抄写一遍，理解每个步骤的作用
- 重点关注Aware接口的设计模式

### 今日实践任务                                                                                                                                                                                                                                                                                                                                                                             

1. **阅读源码**：通读BeanFactory.java，重点关注注释
2. **整理笔记**：画出生命周期时序图
3. **思考题**：
   - 为什么getBean有多个重载版本？
   - FactoryBean前缀`&`的设计意图是什么？
   - Aware接口体现了什么设计模式？

---

## Day 2: ListableBeanFactory 接口解析

### 核心文件
`spring-beans/src/main/java/org/springframework/beans/factory/ListableBeanFactory.java`

### 关键要点

#### 1. 接口定位（第28-32行注释）
```java
/**
 * Extension of the {@link BeanFactory} interface to be implemented by bean factories
 * that can enumerate all their bean instances, rather than attempting bean lookup
 * by name one by one as requested by clients.
 */
```
**理解**：如果说BeanFactory是"按需查找"，ListableBeanFactory就是"批量枚举"。

#### 2. 与BeanFactory的区别

| 特性 | BeanFactory | ListableBeanFactory |
|------|-------------|---------------------|
| 查询方式 | 按名称单个查询 | 批量枚举所有bean |
| 预加载 | 可延迟加载 | 通常预加载所有定义 |
| 典型实现 | SimpleBeanFactory | DefaultListableBeanFactory |

#### 3. 核心方法分类

##### Bean定义相关（第70-89行）
```java
boolean containsBeanDefinition(String beanName);  // 是否包含bean定义
int getBeanDefinitionCount();                     // bean定义数量
String[] getBeanDefinitionNames();                // 所有bean定义名称
```

##### 按类型查找（第161-327行）⭐

**重点方法**：`getBeanNamesForType`有多个重载版本

```java
// 基础版本 - 只考虑当前工厂，不考虑层级
String[] getBeanNamesForType(@Nullable Class<?> type);

// 完整版本 - 控制是否包含非单例、是否允许急切初始化
String[] getBeanNamesForType(@Nullable Class<?> type,
                             boolean includeNonSingletons,
                             boolean allowEagerInit);
```

**关键参数理解**：
- `includeNonSingletons`：是否包含prototype和scoped bean
- `allowEagerInit`：是否允许初始化lazy-init和FactoryBean（⚠️ 设为true可能导致提前初始化）

##### 获取Bean实例集合（第286-327行）
```java
<T> Map<String, T> getBeansOfType(@Nullable Class<T> type);
```
返回的是`Map<beanName, beanInstance>`，**注意这会实际实例化bean**。

##### 注解相关查询（第341-421行）
```java
String[] getBeanNamesForAnnotation(Class<? extends Annotation> annotationType);
Map<String, Object> getBeansWithAnnotation(Class<? extends Annotation> annotationType);
<A extends Annotation> A findAnnotationOnBean(String beanName, Class<A> annotationType);
```

**使用场景**：用于组件扫描、注解驱动的配置。

#### 4. 重要提示（第49-51行注释）
```java
/**
 * <b>NOTE:</b> With the exception of {@code getBeanDefinitionCount}
 * and {@code containsBeanDefinition}, the methods in this interface
 * are not designed for frequent invocation. Implementations may be slow.
 */
```
**警告**：除`getBeanDefinitionCount`和`containsBeanDefinition`外，其他方法不适合频繁调用，可能较慢。

### 今日实践任务

1. **对比学习**：对比BeanFactory和ListableBeanFactory的方法差异
2. **整理表格**：列出所有按类型查找的方法及其区别
3. **思考题**：
   - 为什么`getBeanNamesForType`需要`allowEagerInit`参数？
   - `getBeansOfType`和`getBeanNamesForType`的区别是什么？
   - 什么时候应该用BeanFactory，什么时候用ListableBeanFactory？

---

## Day 3: BeanDefinition 接口解析

### 核心文件
`spring-beans/src/main/java/org/springframework/beans/factory/config/BeanDefinition.java`

### 关键要点

#### 1. 接口定位
BeanDefinition是**Bean的元数据描述**，包含：
- 类名、作用域、是否延迟初始化
- 构造函数参数、属性值
- 初始化/销毁方法名
- 是否自动装配候选等

#### 2. 常量定义

##### 作用域常量（第47, 55行）
```java
String SCOPE_SINGLETON = ConfigurableBeanFactory.SCOPE_SINGLETON;  // "singleton"
String SCOPE_PROTOTYPE = ConfigurableBeanFactory.SCOPE_PROTOTYPE;  // "prototype"
```

##### 角色常量（第62-78行）⭐
```java
int ROLE_APPLICATION = 0;      // 应用程序主要部分（用户定义的bean）
int ROLE_SUPPORT = 1;          // 配置支持部分
int ROLE_INFRASTRUCTURE = 2;   // 完全的后台角色（框架内部使用）
```

**理解**：
- `ROLE_APPLICATION`：业务代码中的@Service, @Controller等
- `ROLE_SUPPORT`：某些复合组件的一部分
- `ROLE_INFRASTRUCTURE`：@Configuration, BeanPostProcessor等

#### 3. 核心属性方法分类

##### 基础属性

| 方法 | 说明 |
|------|------|
| `setParentName/getParentName` | 父bean定义名称 |
| `setBeanClassName/getBeanClassName` | Bean的类名 |
| `setScope/getScope` | 作用域 |
| `setLazyInit/isLazyInit` | 是否延迟初始化 |

##### 依赖相关

| 方法 | 说明 |
|------|------|
| `setDependsOn/getDependsOn` | 依赖的其他bean名称 |
| `setAutowireCandidate/isAutowireCandidate` | 是否自动装配候选 |
| `setPrimary/isPrimary` | 是否是主要的 |
| `setFallback/isFallback` | 是否是备用的（Spring 6.2新增）|

**autowireCandidate vs primary vs fallback**：
- `autowireCandidate=false`：完全排除自动装配
- `primary=true`：多个候选时优先选择
- `fallback=true`：只有其他都排除后才选择

##### 工厂方法相关（第207-234行）

```java
setFactoryBeanName/getFactoryBeanName;      // 工厂bean名称
setFactoryMethodName/getFactoryMethodName;  // 工厂方法名称
```

两种创建bean的方式：
1. **构造器创建**：设置`beanClassName`，直接实例化
2. **工厂方法创建**：设置`factoryBeanName`和`factoryMethodName`

##### 属性值和构造参数（第241-266行）

```java
ConstructorArgumentValues getConstructorArgumentValues();  // 构造参数
MutablePropertyValues getPropertyValues();                  // 属性值
```

##### 生命周期方法（第272-292行）

```java
setInitMethodName/getInitMethodName;        // 初始化方法
setDestroyMethodName/getDestroyMethodName;  // 销毁方法
```

##### 元信息（第302-323行）

```java
setRole/getRole;                  // 角色
setDescription/getDescription;    // 描述
```

#### 4. 只读属性（第326-370行）

```java
ResolvableType getResolvableType();    // 可解析的类型
boolean isSingleton();                 // 是否是单例
boolean isPrototype();                 // 是否是原型
boolean isAbstract();                  // 是否是抽象的（不能实例化）
String getResourceDescription();       // 资源描述
BeanDefinition getOriginatingBeanDefinition();  // 原始定义
```

### 今日实践任务

1. **属性分类**：把所有方法按功能分类整理
2. **对比表格**：列出ROLE_APPLICATION/ROLE_SUPPORT/ROLE_INFRASTRUCTURE的区别
3. **思考题**：
   - BeanDefinition和实际Bean实例的关系是什么？
   - 工厂方法和构造器创建的区别？
   - autowireCandidate、primary、fallback的区别和使用场景？

---

## Day 4: 动手实践

### 实践项目：手动使用DefaultListableBeanFactory

#### 1. 创建测试类

```java
package com.example.beans.practice;

import org.springframework.beans.factory.support.DefaultListableBeanFactory;
import org.springframework.beans.factory.support.RootBeanDefinition;

public class Day4Practice {

    public static void main(String[] args) {
        // 1. 创建BeanFactory
        DefaultListableBeanFactory factory = new DefaultListableBeanFactory();

        // 2. 创建Bean定义
        RootBeanDefinition userServiceDef = new RootBeanDefinition(UserService.class);
        userServiceDef.setScope("singleton");
        userServiceDef.setLazyInit(false);
        userServiceDef.setInitMethodName("init");

        // 3. 注册Bean定义
        factory.registerBeanDefinition("userService", userServiceDef);

        // 4. 获取Bean
        UserService userService = factory.getBean("userService", UserService.class);
        System.out.println("UserService: " + userService);

        // 5. 再次获取，验证单例
        UserService userService2 = factory.getBean("userService", UserService.class);
        System.out.println("是否是同一个实例: " + (userService == userService2));

        // 6. 使用ListableBeanFactory的功能
        System.out.println("Bean定义数量: " + factory.getBeanDefinitionCount());
        System.out.println("所有Bean名称: " + String.join(", ", factory.getBeanDefinitionNames()));

        // 7. 按类型查找
        String[] names = factory.getBeanNamesForType(UserService.class);
        System.out.println("UserService类型的Bean: " + String.join(", ", names));
    }
}

class UserService {

    public void init() {
        System.out.println("UserService初始化");
    }

    public void hello() {
        System.out.println("Hello!");
    }
}
```

#### 2. 进阶练习

**练习1：创建Prototype Bean**
```java
RootBeanDefinition prototypeDef = new RootBeanDefinition(PrototypeBean.class);
prototypeDef.setScope("prototype");
factory.registerBeanDefinition("prototypeBean", prototypeDef);

// 获取两次，验证是否是不同实例
PrototypeBean p1 = factory.getBean("prototypeBean", PrototypeBean.class);
PrototypeBean p2 = factory.getBean("prototypeBean", PrototypeBean.class);
System.out.println("Prototype是否是不同实例: " + (p1 != p2));
```

**练习2：使用别名**
```java
factory.registerAlias("userService", "us");
factory.registerAlias("userService", "userSvc");

// 通过别名获取
UserService us = factory.getBean("us", UserService.class);
System.out.println("通过别名获取: " + us);

// 获取所有别名
String[] aliases = factory.getAliases("userService");
System.out.println("别名: " + String.join(", ", aliases));
```

**练习3：父子Bean定义**
```java
// 创建父定义
RootBeanDefinition parentDef = new RootBeanDefinition();
parentDef.setAbstract(true);
parentDef.getPropertyValues().add("name", "ParentName");

// 创建子定义
RootBeanDefinition childDef = new RootBeanDefinition(ChildBean.class);
childDef.setParentName("parent");

factory.registerBeanDefinition("parent", parentDef);
factory.registerBeanDefinition("child", childDef);
```

### 调试技巧

#### 1. 在关键方法打断点
- `DefaultListableBeanFactory.getBean()`
- `DefaultListableBeanFactory.registerBeanDefinition()`
- `AbstractBeanFactory.doGetBean()`

#### 2. 观察内部状态
- `beanDefinitionMap` - 存储所有bean定义
- `singletonObjects` - 存储单例实例
- `aliasMap` - 存储别名映射

#### 3. 使用IDE的Evaluate功能
在调试时执行：
```java
factory.getBeanDefinitionCount();
factory.getBeanDefinitionNames();
factory.getType("userService");
```

### 验证检查清单

- [ ] 能成功创建DefaultListableBeanFactory
- [ ] 能注册BeanDefinition
- [ ] 能通过名称获取Bean
- [ ] 能通过类型获取Bean
- [ ] 验证了单例行为
- [ ] 验证了原型行为
- [ ] 使用了别名功能
- [ ] 使用了ListableBeanFactory的枚举功能

---

## 常见问题

### Q1: BeanFactory和ApplicationContext的区别？

**A**:
- BeanFactory是基础容器，提供DI核心功能
- ApplicationContext继承自BeanFactory，添加了：
  - 国际化支持（MessageSource）
  - 事件传播（ApplicationEventPublisher）
  - 资源加载（ResourceLoader）
  - 自动注册BeanPostProcessor等

**建议**：学习阶段从BeanFactory开始，理解核心原理后再学ApplicationContext。

### Q2: 为什么要用RootBeanDefinition而不是直接用BeanDefinition？

**A**:
- BeanDefinition是接口，定义契约
- RootBeanDefinition是具体实现
- 实际使用中还有ChildBeanDefinition（继承父定义）、GenericBeanDefinition（通用定义）

### Q3: singleton和prototype的区别？

**A**:
| 特性 | singleton | prototype |
|------|-----------|-----------|
| 实例数量 | 容器中只有一个 | 每次获取新建一个 |
| 创建时机 | 容器启动时（默认）或首次获取 | 每次getBean时 |
| 生命周期管理 | 容器负责销毁 | 容器不管理销毁，需自行处理 |
| 线程安全 | 需要注意 | 每个线程独立实例 |

### Q4: lazy-init有什么用？

**A**:
- `lazy-init=false`（默认）：容器启动时立即创建单例bean
- `lazy-init=true`：首次使用时才创建
- 用途：加快启动速度，按需加载

### Q5: FactoryBean是什么？和BeanFactory有什么区别？

**A**:
- **BeanFactory**：容器，管理bean定义和实例
- **FactoryBean**：工厂bean，是一种特殊的bean，用于创建其他bean
- 获取FactoryBean本身要在名称前加`&`

---

## 扩展阅读

### 下一阶段预告

阶段2将学习BeanDefinition的完整继承体系：
- AbstractBeanDefinition - 抽象基类
- RootBeanDefinition - 根定义
- ChildBeanDefinition - 子定义
- GenericBeanDefinition - 通用定义

### 推荐阅读源码

1. `DefaultListableBeanFactory` - 完整实现
2. `AbstractBeanFactory` - 抽象实现
3. `BeanDefinitionHolder` - 定义+名称的包装

---

## 总结

完成阶段1后，你应该能够：

1. ✅ 解释BeanFactory的核心作用
2. ✅ 列举Bean的生命周期阶段
3. ✅ 区分BeanFactory和ListableBeanFactory
4. ✅ 理解BeanDefinition的属性
5. ✅ 手动使用DefaultListableBeanFactory创建和管理bean

**学习技巧**：
- 边读源码边做笔记
- 多写测试代码验证理解
- 使用IDE调试跟踪执行流程
