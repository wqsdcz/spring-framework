# Spring Bean 创建流程 - 业务逻辑说明

## 概述

`createBean` 方法是 Spring Framework 中负责**创建和初始化 Bean 实例**的核心方法。它将配置信息（BeanDefinition）转换为可用的 Java 对象，并管理对象的生命周期各个阶段。

---

## 整体流程

Bean 的创建过程分为两个主要阶段：

1. **预处理阶段**（createBean）- 准备和校验
2. **实际创建阶段**（doCreateBean）- 实例化、装配和初始化

---

## 一、预处理阶段

### 1. Bean 类解析

首先，系统需要确定要创建的 Bean 的具体类型：

- 根据配置中定义的类名，加载对应的 Java 类
- 如果类是动态解析的（如通过表达式），会创建独立的 Bean 定义副本
- 验证方法覆盖配置（如 lookup-method、replaced-method）的合法性

### 2. 实例化前置处理

在真正创建对象之前，Spring 给**后置处理器**一个机会：

- 调用 InstantiationAwareBeanPostProcessor 的扩展点
- 某些特殊的后置处理器（如 AOP 代理创建器）可能直接返回代理对象
- 如果返回了代理对象，则跳过后续的标准创建流程，直接返回代理

> 这是 Spring AOP 实现"代理替换目标对象"的关键机制。

---

## 二、实际创建阶段（doCreateBean）

### 1. 实例化 Bean

创建 Bean 的原始对象，有以下几种策略：

#### 策略 A：Supplier 创建
如果配置了实例提供者（InstanceSupplier），直接调用 Supplier 获取实例。

#### 策略 B：工厂方法创建
如果配置了工厂方法（静态工厂或实例工厂）：
- 解析工厂方法的参数
- 根据参数类型和名称匹配依赖
- 调用工厂方法创建实例

#### 策略 C：构造器自动装配
如果 Bean 配置了构造器注入：
- 通过后置处理器确定候选构造器
- 解析构造器参数（按类型或名称匹配）
- 选择最优构造器并创建实例

#### 策略 D：默认无参构造器
最简单的情况，直接使用默认的无参构造器创建实例。

> **注意**：对于单例 Bean，会先从缓存中查找是否已有部分创建的实例（FactoryBean 特殊情况）。

---

### 2. 合并 Bean 定义的后置处理

实例创建后，但还未填充属性前，Spring 允许后置处理器检查和修改 Bean 定义：

- 调用 MergedBeanDefinitionPostProcessor
- **典型应用**：@Autowired、@Value、@PostConstruct 等注解的扫描和缓存
- 这一步会将注解信息缓存起来，供后续步骤使用

---

### 3. 循环依赖处理（提前暴露）

如果 Bean 是单例且允许循环依赖：

- 将 Bean 的**对象工厂**提前注册到三级缓存中
- 这个工厂可以在其他 Bean 需要引用该 Bean 时，返回一个**早期引用**（可能还未完全初始化）
- 这是 Spring 解决单例 Bean 循环依赖的核心机制

> 例如：A 依赖 B，B 又依赖 A。创建 A 时提前暴露，B 创建时可以引用到早期的 A。

---

### 4. 属性填充（依赖注入）

将配置的属性值和依赖注入到 Bean 实例中：

#### 步骤 1：实例化后置处理
- 再次调用 InstantiationAwareBeanPostProcessor
- 允许修改 Bean 状态或跳过属性填充
- **典型应用**：@Autowired 字段注入在此阶段完成

#### 步骤 2：按名称自动装配（autowireByName）
如果配置了按名称自动装配：
- 找出所有未满足的非简单类型属性
- 根据属性名查找同名的 Bean
- 将找到的 Bean 设置为属性值
- 注册依赖关系（用于销毁顺序控制）

#### 步骤 3：按类型自动装配（autowireByType）
如果配置了按类型自动装配：
- 根据属性类型查找匹配的 Bean
- 如果存在多个匹配，按优先级选择（如 @Primary）
- 将匹配的 Bean 设置为属性值

#### 步骤 4：属性值处理
- 解析属性值中的运行时引用（如 ${} 占位符、SpEL 表达式）
- 进行类型转换（如 String 转 Integer）
- 将最终值通过 setter 方法设置到 Bean 中

#### 步骤 5：依赖检查（可选）
如果配置了依赖检查：
- 验证所有必需属性是否都已设置
- 如果存在未设置的必需属性，抛出异常

---

### 5. Bean 初始化

属性填充完成后，执行初始化逻辑：

#### 步骤 1：Aware 接口回调
如果 Bean 实现了 Aware 接口，注入相关资源：
- **BeanNameAware**：注入 Bean 的名称
- **BeanClassLoaderAware**：注入类加载器
- **BeanFactoryAware**：注入 Bean 工厂本身

#### 步骤 2：初始化前置处理
- 调用 BeanPostProcessor 的初始化前置方法
- **典型应用**：@PostConstruct 注解的方法在此执行

#### 步骤 3：执行初始化方法
按顺序执行初始化逻辑：

1. **InitializingBean 接口**
   - 如果实现了 InitializingBean，调用 `afterPropertiesSet()` 方法

2. **自定义初始化方法**
   - 执行 XML 或注解中指定的 init-method
   - 可以通过方法名指定任意无参方法作为初始化方法

#### 步骤 4：初始化后置处理
- 调用 BeanPostProcessor 的初始化后置方法
- **典型应用**：AOP 代理在此阶段创建，返回代理对象替代原始 Bean

> **重要**：如果 Bean 需要被代理，此时返回的是代理对象，而非原始对象。

---

### 6. 早期引用校验

如果 Bean 之前被提前暴露（用于解决循环依赖）：

- 检查最终暴露的对象是否与早期暴露的对象一致
- 如果 Bean 被代理，确保循环依赖中的 Bean 引用也被正确更新
- 如果存在不一致（如原始 Bean 被注入，但后续被代理），可能抛出异常

---

### 7. 注册销毁回调

如果 Bean 定义了销毁逻辑：

- 注册 DisposableBean 适配器
- 收集销毁方法（DisposableBean 接口、自定义 destroy-method）
- 当容器关闭时，按依赖顺序逆序调用销毁方法

---

## 三、返回 Bean 实例

最终，创建完成的 Bean 实例（可能是代理对象）会被返回，并进入单例缓存（如果是单例作用域）。

---

## 关键设计思想

### 1. 扩展点丰富
整个创建过程穿插了多个后置处理器扩展点，允许框架和开发者在不修改源码的情况下定制行为。

### 2. 循环依赖解决
通过三级缓存机制（完整单例、早期单例、单例工厂），支持构造器注入以外的循环依赖。

### 3. 代理透明性
AOP 代理的创建对业务代码透明，代理替换发生在初始化阶段末期。

### 4. 生命周期完整
从实例化 → 属性填充 → 初始化 → 销毁，提供了完整的生命周期管理。

---

## 常见应用场景

| 应用场景 | 涉及的步骤 |
|---------|-----------|
| @Autowired 字段注入 | 属性填充阶段的实例化后置处理 |
| @PostConstruct 初始化 | 初始化阶段的初始化前置处理 |
| AOP 代理创建 | 预处理阶段的实例化前置处理 或 初始化后置处理 |
| @Value 配置注入 | 属性填充阶段的属性值处理 |
| @PreDestroy 销毁 | 注册销毁回调阶段 |
| 循环依赖 | 提前暴露阶段 + getSingleton |
